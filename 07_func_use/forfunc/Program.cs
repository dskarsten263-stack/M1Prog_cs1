namespace forfunc;

class Program
{

    static void Main(string[] args)
    {
        
        int value1 = 2;
        for (int power = 2; power <= 2; power++)
            Console.WriteLine($"{value1} keer zichzelf {Math.Pow(value1, power):N0} ");
            
        
        int value2 = 5;
        for (int power = 2; power <= 2; power++)
            Console.WriteLine($"{value2} keer zichzelf {Math.Pow(value2, power):N0} ");
        
        int value3 = 78;
        for (int power = 2; power <= 2; power++)
            Console.WriteLine($"{value3} keer zichzelf {Math.Pow(value3, power):N0} ");
   
        int value4 = 98;
        for (int power = 2; power <= 2; power++)
            Console.WriteLine($"{value4} keer zichzelf {Math.Pow(value4, power):N0}");
   
    }
}
